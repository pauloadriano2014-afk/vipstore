<style>
  .banner-full-width {
    width: 100vw;
    position: relative;
    left: 50%;
    right: 50%;
    margin-left: -50vw;
    margin-right: -50vw;
    height: 600px; /* Altura fixa para Desktop */
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }
  .banner-img {
    position: absolute;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 1;
  }
  .banner-content {
    position: relative;
    z-index: 3;
    text-align: center;
    color: #fff;
    max-width: 800px;
    padding: 20px;
  }
  .banner-overlay {
    position: absolute;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,0.4);
    z-index: 2;
  }
  
  /* Estilo do Botão Rosa */
  .banner-btn {
    display: inline-block; 
    padding: 15px 40px; 
    background: #D4AF37; /* Rosa Menina Chique */
    color: #ffffff; 
    text-decoration: none; 
    font-weight: bold; 
    margin-top: 20px;
    border-radius: 4px; /* Cantos levemente arredondados */
    text-transform: uppercase;
    transition: background 0.3s ease; /* Efeito suave */
    border: none;
    cursor: pointer;
  }

  .banner-btn:hover {
    background: #D1107A; /* Rosa um pouco mais escuro ao passar o mouse */
    color: #fff;
  }
  
  /* Ajuste para celular */
  @media (max-width: 768px) {
    .banner-full-width { height: 500px; }
    .banner-content h1 { font-size: 2.5rem !important; }
  }
</style>

<div class="banner-full-width">
  {% if section.settings.image != blank %}
    <img src="{{ section.settings.image | img_url: '1800x' }}" alt="Banner" class="banner-img">
  {% else %}
    <div class="banner-img" style="background:#333;"></div>
  {% endif %}
  
  <div class="banner-overlay"></div>
  
  <div class="banner-content">
    <h1 style="font-size: 3.5rem; font-weight: bold; margin-bottom: 10px; text-transform: uppercase;">{{ section.settings.title }}</h1>
    
    <a href="{{ section.settings.button_link }}" class="banner-btn">
      {{ section.settings.button_label }}
    </a>
  </div>
</div>

{% schema %}
{
  "name": "Banner Full Width",
  "settings": [
    { "type": "image_picker", "id": "image", "label": "Imagem" },
    { "type": "text", "id": "title", "label": "Título", "default": "SUA MELHOR VERSÃO" },
    { "type": "text", "id": "button_label", "label": "Botão", "default": "COMPRAR AGORA" },
    { "type": "url", "id": "button_link", "label": "Link" }
  ],
  "presets": [{ "name": "Banner Full Width" }]
}
{% endschema %}